# Streamlit
streamlit>=1.30.0

# Data manipulation
pandas>=2.1.0
numpy>=1.26.0

# Plotting & visualization
matplotlib>=3.8.0
seaborn>=0.12.3
plotly>=5.20.0

# Optional: LLM/NLP or analytics extensions
# openai>=1.0.0
# nltk>=3.8.1
# scikit-learn>=1.3.0

# Code quality / testing (for dev workflows)
flake8>=6.1.0
pytest>=8.2.0

# CSV / Parquet (if needed)
pyarrow>=12.0.0
fastparquet>=2023.12.0


# langdetect – For detecting the language of each summary to apply language-specific themes.
langdetect>=1.0.9

# Required for saving and reading Parquet files efficiently.
pyarrow>=12.0.0

# OpenAI API client
openai>=1.0.0

# Data handling
pyarrow>=11.0
 #fastparquet>=2023.7

# Progress bars
tqdm>=4.65

# Environment variable support
python-dotenv>=1.0

#faster and more accurate language detection,
langid>=1.1.6

# For Lambda DB and S3 access
pymysql
boto3

google-auth                # Core Google authentication library (handles service accounts, OAuth tokens)
google-auth-oauthlib       # OAuth 2.0 flow helpers for installed apps or web apps (used when not using service accounts)
google-auth-httplib2       # HTTP transport layer used by google-api-python-client for authenticated requests
google-api-python-client   # Main Google API client (used to upload files to Google Drive, interact with Drive API v3)

dash   
altair
streamlit-dash
streamlit_plotly_events
